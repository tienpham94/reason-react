Notes on ReasonReact

- Instead of class declare a `make` function
- In Reason, the type system restricts you from passing arbitrary data like so; you can only return ReasonReact.reactElement from render.
